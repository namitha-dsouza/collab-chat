# collab-chat

A real-time collaborative chat app built with Node.js, React, PostgreSQL, and Redis.

## Tech Stack

- **Frontend** — React + TypeScript + Vite + Tailwind CSS
- **Backend** — Node.js + Express + TypeScript
- **Database** — PostgreSQL (via Prisma ORM)
- **Cache** — Redis
- **Real-time** — Socket.io
- **Auth** — JWT + refresh tokens

## Features (in progress)

- [ ] User authentication (register/login)
- [ ] Create and join chat rooms
- [ ] Real-time messaging
- [ ] Typing indicators
- [ ] Online presence

<!-- ## Local Setup

1. Clone the repo
   ```bash
   git clone https://github.com/YOURUSERNAME/collab-chat.git
   cd collab-chat
   ```

2. Start databases
   ```bash
   docker compose up -d
   ```

3. Set up the server
   ```bash
   cd server
   cp .env.example .env
   npm install
   npx prisma migrate dev
   npx ts-node src/index.ts
   ```

4. Set up the client
   ```bash
   cd client
   npm install
   npm run dev
   ```
 -->
